import request from "supertest";
import app from './app.js'; // Adjust the path to your app.js file

describe("Subscription Service API", () => {
  describe("Authentication Routes", () => {
    it("POST /auth --> login user", () => {});
  });
});
